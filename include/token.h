#ifndef TOKEN_H
#define TOKEN_H

#define MAX_NUM_TOKENS 1000

// Tokenize inputLine and store pointers in token[]
// Returns number of tokens found, or -1 if too many tokens
int tokenise(char *inputLine, char *token[]);


#endif // TOKEN_H